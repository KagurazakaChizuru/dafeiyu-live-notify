# 更新记录

版本号是 `主.次.修订`：**次**版本加功能，**修订**只修 bug。

> **关于 1.4.2 和 1.5.1**：这两版**在本地构建过、exe 也部署过，但没有单独发版**——
> 改动被并进了紧随其后的 1.5.0 和 1.5.2。功能一行没少，只是编号从发布历史里
> 跳过了。这两节保留下来是为了让改动有据可查；GitHub 上不会有对应的 Release。

---

## 1.8.0

### 新功能：UP 主发新视频 / 新动态也通知

原来两类通知都在说"我开播了"。这一版加了第三类：**我关注的人更新了** ——
投稿和动态都能通知。「触发方式」页最下面填个 UID 就能用；**投稿不需要登录**。

```
🔔 某某 更新了

新视频的标题
https://www.bilibili.com/video/BV1xx411c7mD
```

新文件 `bili.py`（纯标准库、只读）负责拿数据：投稿要 wbi 签名 + buvid 指纹 +
`dm_img_*` 探针参数，三样缺一样就只会得到一个看不懂的风控码。

**动态必须登录，这一点是测出来的**（2026-09-21，带对照组）：老接口
`dynamic_svr/space_history` 已 404；`feed/space` 匿名要么回 `-352`、要么回
`code=0` 但 `items` 是空数组；**B站官方号（mid=2）结果一模一样** ——
不是账号问题，是匿名一律不给。所以动态要一次 `subscribe.sessdata`：
日志里打码、界面不回显（跟控制端口 token 一个口径）、`_package.ps1` 按内容扫
`SESSDATA` 拒绝打包。**凭据过期不报错，这条是踩出来的**：拿一个假的 SESSDATA 打真实接口，返回的是
`code=0` + 空的 items —— 跟"这个 UP 主最近没发动态"长得一模一样。也就是说凭据
死了会**静默失效**，正是这类功能最难查的失败方式。所以：

- `Space.sessdata_looks_ok()` 拿 B站官方号（动态不断）做对照读一次，
  读得到才算有效；`check` 在动态开着时自动跑这一下，失效会明说。
- `-352` / `-401` 的报错文案改准了：那两个码是风控和"没带登录态"，
  **不是**过期（原来写的是"过期了？"，会把排查方向带偏）。
- 使用文档里告诉用户：动态突然不来了，双击 `1-自检.bat` 就知道。

判断规矩跟开播那边一致，都写进了自检：

- **首次订阅不补发历史。** 第一次见到某个 UP 主时只把当前最新一条记成基线，
  一条都不播报 —— 否则等于把人家几年的旧投稿一次刷进群。
- **按发布时间（`created`）比对**，不按"在列表里的位置"：列表随翻页滚动，位置会变。
- 积压超过 3 条时只播报最新一条，并记一行 WARN。
- **投稿类动态自动跳过** —— 它跟投稿是同一件事，不跳就会同一个视频报两遍。
- 动态正文压平换行、截到 140 字（优先在标点处断）：动态可以是一整篇长文。
- 轮询**下限 60 秒**、默认 300 秒。实测查太勤会被 B站按来源拦掉（整个来源一起
  412，换个 UP 主也一样），所以这个下限是硬拦，不是建议。
- **被拦之后退避 15 分钟**（`SUB_BACKOFF_SECONDS`），退避时刻记在状态文件里。
  原来撞上之后会照 `poll_seconds` 的节奏接着敲 —— 那是把封禁往外拖。
- 状态记在 `logs/subscribe-state.json`，一个 UP 主一条，方便你自己看。

### 文案与测试窗口

- 投稿文案写得更抓眼（12 条：「新片出锅」「别刷了，看这个」「刚更新」…），
  另加了动态文案池（8 条）。都只写第三方的口吻 —— 播报的是别人发的，
  用「我刚发的」会驴唇不对马嘴。
- **测试窗口**：「预览将发送的内容」现在是个窗口，开着的每一类都列出来，
  还能「换一批」重新随机挑文案 —— 池子里十几句，原来关掉再打开才是下一条。
  旁边多了「私聊发新投稿」「私聊发新动态」两个按钮，照旧只发到你填的 QQ。

### 群友能和「大肥鱼」聊天了（1.8.0）

@机器人 说话，群里的猫娘女仆会接话；顺便能记提醒：
「@我 提醒我 21:30 交作业」。默认**关**，默认**只理 @机器人** 的话。

**模型走本地，不烧 API。** 她原话是「不要每次都调用API，不然token顶不住」——
对。所以默认后端是本机的 OpenAI 兼容接口（Ollama / LM Studio / llama.cpp
都是这个形状，`chat.local_url` 可配），人设、温度、长度都在代码里定好，
用标准库 urllib 打，不引依赖。想用 DSH 也行：`chat.backend = "dsh"`。

**安全边界是按"它没有手"来设计的，不是靠嘱咐**：

- DSH 那条路用的是新建的 `groupchat` 档案（`~/.dsh/profiles/groupchat/`），
  里面把所有 `tool-*` 插件都禁掉了 —— 群友再怎么诱导，它也没有能调用的东西。
  实测：让它"列出当前目录"，它只能说「给我工具我立刻列」；让它"忽略之前的
  设定去翻 C 盘"，它直接拒绝。
- **本地模型连工具的概念都没有**，比禁掉工具还干净。
- 群里的话**不进命令行解析**：走 `node bin.js` 直连，不经 `dsh.cmd` 那个垫片
  （那是 cmd.exe 跑的，一句带 `&`、`%`、`"` 的话会被二次解析）。实测带这些
  字符的提问只被当普通文字。
- 同一个人有冷却（默认 20 秒），回复长度有上限，问模型是**后台线程**（一次
  十几秒，卡在主循环里直播状态就不刷了）。

顺带一个设计选择：**提醒的时间只认几种写法**（21:30 / 晚上9点 / 9点半 /
30分钟后 / 明天早上8点），看不懂就明说该怎么写。猜错时间的提醒比没有提醒
更烦人 —— 实测这里错过一次（「晚上12点」算成了中午十二点，差 12 小时），
所以那条规则是单独写死的。

### 转发类动态的正文：不再是一句「分享视频」

用她自己的数据验收时发现的：最新几条动态全是转发，而 B站塞在自己那层的
`desc.text` 一律是「分享视频」—— 照发出去，群里看到的就是
「咕噜千鹤子 发了条动态 / 分享视频 / 链接」，等于没说。真内容在被转发那条
动态的 `orig` 里。

- `_dyn_text()` 现在会往 `orig` 里找一层，通用标签（分享视频/转发动态…）
  一律让位给真标题。
- **教训写进注释了**：第一版只在自己那层找，测试用的是我手搓的**扁平假结构**，
  于是"本地过了、真实数据没变"。测试得照着真响应写 —— 这条比修复本身值钱。

### 头图改成自适应缩放（用户报的）

用户把窗口拉宽之后，头图右边露出一条浅蓝白 —— 因为它是 **780 定尺图**贴在左上角，
而窗口比它宽，露出来的那条接不上头图右缘的饱和蓝，看着像"图缺了一块"。
用户一句话点到根上：「你不能定死他的大小啊，应该自适应啊」。

- 现在按窗口尺寸**等比缩放**（cover：盖满头部后居中裁切），布局高度不动。
- tkinter 的 `PhotoImage` 只能整数倍 zoom/subsample，项目又不引第三方依赖 ——
  所以走 **Windows 自带的 GDI**（`StretchBlt` + `HALFTONE`，纯 ctypes，
  `tray.py` 早有 ctypes 调 Win32 的先例）。纯 Python 逐像素双线性对 17 万像素
  要一两秒，拖窗口会卡成幻灯片；GDI 实测 **3 毫秒**。
- 像素不是逐个 `PhotoImage.get`（十万次 Tcl 调用），而是把图**导出成 PPM**
  再解析；画回去也是拼一份内存里的 PPM 直接建图。
- 过程中踩了两个坑，都值得记：
  · **颜色反了**（蓝头发变金褐色）：24 位 DIB 的内存布局按定义是 BGR，
    而 PPM 给的是 RGB。修复时一开始靠"进出各换一次正好抵消"——那种写法不能留，
    看着像对的、改一处就崩，所以现在是进去换一次、出来换一次，各写明白。
  · `_bg_under`（主题按钮那块方底色）必须取**此刻真正贴着的那张图**并补上贴图
    偏移，否则按钮背后会浮出一个方块（1.7.x 踩过同一个坑）。
- 自检钉了三条：能按尺寸缩放且盖满、缩放后不是原来那张定尺图、
  **缩放耗时 < 0.5 秒**（防哪天有人改回 Python 逐像素）。
- 验证方式不是"看着对"：把窗口拉到 984 宽截图量过 —— 修前 `row y=60`
  在 x=788 处开始平坦（图到那儿就断了），修后整行**没有任何平坦段**。

### 加了登录接口：不用再去 F12 抄 cookie（1.8.0）

动态要登录态，但上一版的做法是"自己去浏览器 F12 抄 SESSDATA 填进 config.json" ——
那不叫登录。现在「触发方式」页订阅卡里有个**「登录 B站（扫码）」**：
点开是一张二维码，手机 B站扫一下、确认，登录态就存好了，界面那行小字会变成
「B站登录态：已保存」。

- `bili.QrLogin`：passport 的 generate → poll 两步，状态 `wait` / `scanned` /
  `ok` / `expired`。`fetch` 可以注入，所以状态机能不联网单测。
- `qr.py`（新文件）：**自己写的二维码编码器，纯标准库**。项目不引第三方依赖，
  而扫码需要把那条 131 字的 URL 画成码。只实现用得到的那一小块：字节模式 +
  等级 L + 版本 1~10 + 八种掩码按罚分挑。
- **编码器是逐格验证过的**：拿参考实现（`qrcode`，只装在开发机上、不进产品）
  比对，26 条内容 × 8 个掩码 + 60 条随机内容 × 8 个掩码 = **688 次比对，零差异**。
  验证过程里揪出自己三个 bug：填充字节从 `0x11` 起（该 `0xEC`）、格式信息横竖
  两轴写反、定位图案外圈被涂黑（分隔带没了）—— 后两个都是"看着像二维码、
  其实不合规"，只有逐格比才看得出来。自检里钉了一个 21×21 的固定向量。
- **没有去读浏览器的 cookie 库**。那是从别人的加密库里挖凭据，一个公开分发的
  工具不该长那样。扫码是 B站自己的登录方式，也不需要用户装任何东西。

另外：自检里那条 `on_close 收进托盘` 的定位写得太松（`def on_close`），
新加的弹窗里也有个局部 `on_close`，于是它命中了错的那个 —— 改成
`def on_close(self)`。这是"测试写法"的坑，不是产品问题，但它红得很有价值。

### "我开了，但它不推送"——保存路径上的两处坑

2026-09-21 实测（用户报的）：勾了订阅开关、公告里也确实"开着"，可群里一条都没有。
查下来开关**从没进过配置文件**，两个原因叠在一起：

- **「保存设置」只在「消息与设置」页最下面**，而订阅开关在「触发方式」页。
  用户勾完就在这一页找保存，找不到。现在两页各有一个，按的是同一个处理。
- **名单的「添加」是"立刻落盘"，但它不读卡上的开关** —— 它直接 dump 内存里的
  `self.cfg`。于是"勾上开关 → 点添加"写下去的仍是旧的 `enabled: false`，
  界面却显示勾着。现在这条路径会先把两个开关读进来（校验也共用一份）。

顺带把"要不要重启监控"这件事说清楚了：保存走的是**就地 update**，
而监控线程捕获的是 `cfg["subscribe"]` 这个**引用**，所以开关存下去之后，
正在跑的轮询下一轮就读到了 —— **不用重启**（自检里有一条断言钉着这个因果）。
反过来，「放弃修改并重载」会**替换**整个 cfg 对象，那时监控不会自动跟上，
所以那条路现在会明说"要停一次再开始"。

### 点开就崩的文案库

`tk.Frame(..., pady=(0, 12))` —— 控件自身的 `-padx/-pady` 只吃**单个**距离，
Tk 收到 `"0 12"` 就抛 `bad screen distance`。而 `.pack(pady=(0, 12))` 完全合法、
两者长得几乎一样，所以这个 bug 从文案库存在那天起就在，只是**要点开才走到**。
2026-09-21 20:19 用户点出来才知道（截图上的报错就是它）。

补了两层，都写进自检：

- 静态：用 AST 扫"tk/ttk 控件构造调用里出现元组形态的 padx/pady"。
- 动态：**真的把每个文案库窗口和测试窗口打开一次再关掉** —— 这一条走的是
  用户那条路，静态扫描那种写法检查抓不住"点开才崩"。

### 顺手修掉的坑

- **空间接口不能"把请求头补全"。** 带上 `Accept` / `Accept-Language` 会被 WAF 判成
  伪造请求，回 HTTP 412 `{"code":-412,"request was banned"}` —— 文案看着像
  "被限流了"。实测同一个 URL、同一份 cookie：带上就 412，去掉就 `code=0`
  （171 条投稿），两次独立复现。所以 `bili.py` 只发 `User-Agent` + `Referer`：
  看着像漏了，其实是刻意的，自检里有静态断言禁止再补回来。
- 技术文档里平台轮询与 `platform_proxy` 两处写着"见 §11.3 T17"，而 T17 从来没写过 ——
  代理那个陷阱其实是 **T9**。引用改正，T17 / T18 这次补上（就是上面这两条）。
- 技术文档 §5.1 里有两个 `5.1.2`，后面的小节编号顺次挪了一位。

### 界面

- 「触发方式」页多了第五种触发：UP 主名单（**增删即存盘**）与轮询间隔。
  加完人**不替你勾开关** —— 一个会往外发请求的功能，不该因为点了「添加」就自己跑起来。
- 「消息与设置」页多了「UP 主发新视频时的文案」多行框（多条用 `---` 分隔），
  文案库多一类（6 条），占位符 `{up}` / `{title}` / `{link}`。
- 勾了开关却没加人时保存**当场拦下**并说明去哪加。不拦的话 `load_config` 会把开关
  归一成关闭，界面却还显示勾着 —— 用户以为在订阅，其实早就关了。

### 自检

17 组、271 条断言（原 16 组 / 144 条）。新增的都是能反证的：第一次轮询只记基线
不播报、第二次把新投稿发出去、积压只发最新一条、关掉的 UP 主一个请求都不发、
取投稿失败只跳过不抛异常且**退避 15 分钟**（退避期内一个请求都不发、到点恢复）；
配置里 UID 写成字符串照样读、`poll_seconds` 下限抬到 60、没有 UP 主时开关被强制
关掉、没有凭据时动态被强制关掉、整块 `subscribe` 缺失的老配置照常载入；
凭据对照逻辑（有效 / 读不到 / 取不到三种）、动态正文四种摆法都能抠出来、
没有凭据时取动态当场报错（不返回空列表装作没动态）、
`clip_text` 的截断、动态与投稿两条基线分开记、跳过投稿类动态、没开动态就不发动态请求；
界面保存不吃掉 `subscribe.at_all` / `sessdata` 和名单、勾了开关却没名单当场拦下、
勾了动态却没凭据也当场拦下、"立刻落盘"那条路会把开关写进**文件**（读文件核对）、
就地 update 之后正在跑的轮询立刻开始查、二维码固定向量逐格一致、登录状态机四种
状态与被挡后的报错、扫码登录窗口真的点得开、头图按窗口缩放且是毫秒级、群聊的时间解析（八种写法）、只理 @ 了机器人的那条、
同一条消息不重复处理、提醒到点会发出来、**问 DSH 那条命令不带 shell**、
默认走本地模型且带人设、开关是就地生效的、控件构造里没有元组形态的 padx/pady、每个文案库窗口
与测试窗口都真的开得起来、测试窗口里能列出「新动态」。第 16 组用假客户端，**不联网**。

还补了一条一直没人管的：**内置文案池从没被扫过占位符**。`check` 只校验
配置里写的模板，而池子（`TEMPLATE_POOLS` / `HOOK_POOL`）是自己代码里写死的 ——
里面写错一个占位符不会报错，只会把那半句话整行删掉，群里少一句而 `check` 照样
说"都认识"。给订阅加池子时顺手补上，连新投稿那 6 条能不能渲染出内容一起验。

顺手修了自检自己的一处机器相关：控制端口那一组用裸 `urllib.urlopen` 发请求，
而 Windows 的代理例外里通常只有 `localhost.*`、**没有 `127.0.0.1`** ——
代理一开着，这一组整组全红（12 条），报错是 "timed out"，看着像端口没起来。
产品代码一直用 `ProxyHandler({})` 绕开（§11.3 T9），漏的是测试这一边。

## 1.7.6

### 保存设置不再吃掉配置

两处真事故，都是「点一次保存，自己写的东西就没了」，而界面还显示「已保存」：

- `change_templates`（换游戏文案池）与 `reminder.templates`（二次提醒文案池）
  不在保存时写回的键清单里 —— `_ui_to_cfg` 对那几个配置段是**整块替换**的。
  改成就地 update。
- `load_config` 不返回 `ui` 段，而保存是把 `self.cfg` 整个 dump 回文件的，
  于是每次保存都把 `ui` 从 config.json 里抹掉：主题偏好在「保存 → 重启」后
  回落浅色。`behavior.test_target` 同理 —— 它连加载都没解析过。

### 一票静默失效

- 冷却闸门的放行与记账原来分成 `allow()` 和 `mark()` 两次加锁，中间别的触发源
  也能通过 —— 「OBS 开始推流」和「顺手按了快捷键」同时到达就会双发。
- OBS 断线时只置 `connected=False`，`streaming` 留着 True，重连后报回来的
  状态被去重逻辑吞掉 —— **「OBS 重启之后再开播」永远不触发**。
- 握手只看了一眼状态行里的 `101`，而技术文档声称校验了
  `Sec-WebSocket-Accept`；分片消息读到第一片就返回，续帧被当未知帧丢掉。
- `games.py` 的 WeGame `rail_apps` 被列在噪音表里，那条专门处理它的分支
  **永远不可达** —— docstring 写着的主路径一次都没跑过。
- 快捷键没有 `MOD_NOREPEAT`，按住不放会连续触发、刷日志。
- 二次提醒与换游戏播报硬依赖平台轮询：只用 OBS 或进程检测的人永远收不到这两类，
  而且没有任何提示。
- 开播文案没走 `pick_from(kind="live")`，1.7.5 加的深夜池与周末池**没有任何
  调用方** —— 界面上写着按时间自动挑，实际永远挑不到。
- 全丢时 `note_lost()` 一个调用点都没有，四类通知里只有开播那条会响。

### 会骗人与会崩的

- 彩排会把「上次通知」写成「成功 N/N」，与「一条都没发」正好相反；
  `manual_trigger` 无论发没发出去都返回 `ok:true`。
- 6 处数值转换没包 `try`：手滑写错一个值，程序以裸 traceback 退出。
- 13 处 `bool(x)` 归一：**`bool("false")` 是 True** —— 手改配置写一个字符串
  `"false"`，开关会反着走，而界面上看着还是关的。

### 安全

- `/status` 与 `/health` 原来免鉴权。它们会带出群号、当前在玩什么、直播间标题，
  端口虽然只绑 `127.0.0.1`，本机任何程序都读得到。现在与 `/trigger` 一个口径，
  界面轮询同步带上凭据（顺带修掉它硬编码 8899 的毛病）。
- 打包时排除 `_privacy.txt` —— 1.7.5 的标准包把它带上去了，那里面是真实房间号、
  机器人 QQ、控制端口 token 前缀。

### 界面

- `ScrollFrame` 的滚轮从 `bind_all` 改绑顶层窗口，并在销毁时解绑：它挂在解释器的
  `all` 绑签上，控件销毁**不会**解除，换一次主题就多一条死回调，下一次滚动撞
  `bad window path name`，弹「出了点问题」。实测每重建一次泄漏四条。
- 「已保存」那两处 `after` + lambda 改成查当前控件的方法，不再摸已经销毁的标签。

### 自检

16 组、144 条断言。新增的都是能反证的：8 个线程抢冷却闸门只许放行一次、
保存一次之后两个文案池仍在、`ScrollFrame` 销毁后不留滚轮绑定、建界面期间不弹
任何对话框。测试配置也改成带进程号 —— 两份自检同时跑不会再互相删文件。

## 1.7.5

### 开播消息里能看到真实的直播间标题和公告

`{room_title}` 上一版就加了占位符，**但数据线从来没接** —— 开播发送处只传了
`{"game": name}`。所以那个占位符渲染出来永远是空的，而且因为不在「空值删
整行」名单里，它在消息中间**留一个空行**。

现在接上了，并补了 `{room_desc}`（直播公告）。**公告从接口拿到的是 HTML**
（实测返回 `<p>…</p><br>`），直接塞进 QQ 消息群友会看到一堆尖括号，
所以做了剥标签、还原实体、压缩空白，超长还会在标点处截断。

### 游戏播报：冷却的语义本来就是错的

原来是「距上次播报多久内不再播」。玩 A 几分钟不想玩了换 B，是**正常换
游戏**，不该被当成刷屏压掉 —— 压掉之后群里还以为你在玩 A。

冷却的本意是防抖，不是禁言。拆成两件不同的事：

| 参数 | 管什么 | 默认 |
|---|---|---|
| **换游戏确认** | 新游戏要稳定这么久才播报 —— 防切窗口、加载地图的瞬间抖动 | **20 秒** |
| **换游戏冷却** | 两条播报别挨得太密 | **0**（原来 5 分钟） |

### 文案全部重做

**不再写死时段。**「今晚播了」在早上八点下播就是笑话，同类的还有「深夜档」
「周末到了」「上班摸鱼」。基础池只留任何时刻都成立的句子；需要时段的挪进
`live_night` / `live_weekend` / `offline_night`，**按当前时间**并入 ——
判断放在每次挑文案的那一刻，不是加载配置时（配置只读一次，程序会一直挂着
跨过 23 点）。新增 `{tod}` / `{weekday}` 占位符。

**数量也上去了。** 开播 10 → 24 条、群文案 10 → 18 条、下播 6 → 14 条，
emoji 用足。

**换游戏说清楚从什么换成什么**（`{prev_game}`）。上一个游戏认不出来时，
把提到它的模板**整条排除**，而不是塞个假名字 —— 试过塞「刚才那个」，
渲染出「（刚才在玩《刚才那个》）」这种废话。

**群文案（`{hook}`）单独成池。** 模板是消息骨架，群文案是顶端那句勾人的话；
混在一起写，改一句开场白就得重排整个模板。

**下播有了独立分项** —— 原来只有一个单行输入框，连第二条文案都存不下。
现在跟开播对等：多行框 + 文案库 + 多条随机。

### 测试功能

原来想看一眼只有两条路：点 `2-彩排.bat`（命令行），或者真发一次让群友当
小白鼠。

- **预览** —— 只渲染，一条都不发。几类消息一次看全，连「取不到标题时」
  长什么样都能看。
- **私聊测试** —— 真的发出去，但走 `send_private_msg`，不进任何群。

### 托盘图标

收进托盘就不占任务栏，右键退出。**自己用 ctypes 写的**（pystray 会拖进
Pillow，光那一项就把单文件 exe 撑大十几兆）。

难点是托盘要收窗口消息，而**消息得有人泵，Tk 只泵它自己的窗口**。最后选了
自己开线程建隐藏窗口 + 跑自己的 `GetMessage`，跟 Tk 完全隔离。

坑：`wintypes.WPARAM/LPARAM` 在 64 位 Python 下**仍是 32 位**，而真实的是
指针宽度；鼠标消息的 lparam 里塞着屏幕坐标，一超范围就在 `DefWindowProcW`
上抛 `OverflowError`。**这是 ctypes 写 Win32 最经典的坑。**

### 界面绘制提速

用户反馈「有点卡和迟钝」。量下来不是周期任务（最贵才 0.79 ms），是
`button_image` **对整张圆角矩形逐像素跑 Python 循环**：

```
_button_mask        72 ms
PhotoImage + put   131 ms
--------------------------
合计               208 ms / 张
```

拖窗口时宽度每变 1px、十几张卡片全部重算 = **846 ms 一帧**。

改成「内核一次填满 + 外圈逐像素」：**208 → 47.5 ms**，加上 80ms 防抖之后
拖动过程完全不重画。**215990 个像素逐一比对，0 处误差。**

### 自检 14 组 → 16 组

- 第 15 组「控件的引用与创建必须对得上」
- 第 16 组「main() 的接线必须完整」

两组都为具体事故而加，**都做过反证**（删掉对应代码，测试当场报错）。

另外把自检里的固定端口改成让系统分配：原来那个端口撞上 Windows 为 Hyper-V
预留的排除区间，直接 `WinError 10013`，报出来却是「自检失败」——
**测试基础设施的问题伪装成了产品问题。**

### 打包

- 网盘版新增，内置 NapCat，解开双击「首次使用-点我.bat」即可
- 排除 `config.json.bak*`（用户配置的备份，含真实房间号）
- **打包时重写 `loadNapCat.js`** —— NapCat 每次启动都会把它改回绝对路径，
  改一次没用
- 剔掉 `banner-art.png`（1 MB，用户端用不上）

---

## 1.7.3

### 开播消息里终于能看到真实的直播间标题和公告

`{room_title}` 这个占位符上一版就加了，**但数据线从来没接** —— 开播发送处
只传了 `{"game": name}`。所以那个占位符渲染出来永远是空的。

更糟的是它不在「空值删整行」的名单里，于是在消息中间**留一个空行**。

现在接上了，并且补了 `{room_desc}`（直播公告）。**公告从接口拿到的是 HTML**
（实测返回 `<p>…</p><br>`），直接塞进 QQ 消息里群友会看到一堆尖括号，
所以做了剥标签、还原实体、压缩空白，超长还会在标点处截断。

还有个坑值得记一笔：`message.title` 是空的，而用户配置里那份十几条的文案
列表**全都引用 `{title}`** —— 于是每条消息都带一个空行。顺手把 `title` 也
加进了「空值删整行」。

### 游戏播报：冷却的语义本来就是错的

原来是「距上次播报多久内不再播」。这个语义有问题 —— 玩 A 几分钟不想玩了
换 B，是**正常换游戏**，不该被当成刷屏压掉。压掉之后群里还以为你在玩 A。

冷却的本意是防抖（A→B→A→B 来回跳），不是禁言。所以拆成两件不同的事：

| 参数 | 管什么 | 默认 |
|---|---|---|
| **换游戏确认**（新增） | 新游戏要稳定这么久才播报 —— 防切窗口、加载地图的瞬间抖动 | **20 秒** |
| **换游戏冷却** | 两条播报别挨得太密 | **0**（原来是 5 分钟） |

两个都在「消息与设置」里可调。

### 文案不再写死时段

用户报的：「早上下播，文案还是晚上的」。病根在文案池的硬编码 ——
`今晚播了 {duration}` 在早上八点下播就成了笑话。同类的还有「深夜档」
「周末到了」「上班摸鱼」。

基础池现在只留任何时刻都成立的句子；需要时段的挪进 `live_night` /
`live_weekend` / `offline_night`，按**当前时间**并入。判断放在每次挑文案的
那一刻，不是加载配置时 —— 配置只读一次，而程序会一直挂着跨过 23 点。

新增 `{tod}` / `{weekday}`，想自己写时段文案也有词可用。

### 换游戏的通知说清楚是「从什么换成什么」

原来只有「换游戏了，现在打《X》」，看的人不知道之前是什么，也就感觉不到
「换了」。而且 `state["game"]` 在发送**之前**就被覆盖，旧名字当场就丢。

加了 `{prev_game}`。上一个游戏认不出来时，**把提到它的模板整条排除**，
而不是塞个假名字 —— 试过塞「刚才那个」，渲染出「（刚才在玩《刚才那个》）」
这种废话。

### 群文案（`{hook}`）—— 跟模板分开

模板是消息骨架（有哪几行、什么顺序），群文案是顶端那句勾人的话。
混在一起写，改一句开场白就得重排整个模板。所以单独成池，内置十条。

### 测试功能：不打扰群友也能看消息长什么样

原来想看一眼只有两条路：去点 `2-彩排.bat`（命令行），或者真发一次让群友
当小白鼠。

- **预览** —— 只渲染，一条都不发。几类消息一次看全，连「取不到标题时」
  长什么样都能看。
- **私聊测试** —— 真的发出去，但走 `send_private_msg`，不进任何群。

### 托盘图标：收进托盘就不占任务栏

**自己用 ctypes 写的，不引第三方库** —— pystray 会拖进 Pillow，光那一项
就把单文件 exe 撑大十几兆。

难点在于托盘要收窗口消息，而**消息得有人泵，Tk 只泵它自己的窗口**。最后
选了自己开线程建隐藏窗口 + 跑自己的 `GetMessage`，跟 Tk 完全隔离。

踩到的坑：`wintypes.WPARAM/LPARAM` 在 64 位 Python 下**仍是 32 位**，而真实的
是指针宽度；鼠标消息的 lparam 里塞着屏幕坐标，一超范围就在 `DefWindowProcW`
上抛 `OverflowError`。**这是 ctypes 写 Win32 最经典的坑** —— 不声明 argtypes
就按默认宽度猜。现在所有用到的 Win32 函数都补了签名。

语义：点 X 收进托盘（不退出），右键托盘显示/隐藏、开始/停止监控、退出。
**退出只走托盘那一条**，误点不了。

### 自检 14 组 → 16 组

- 第 15 组「控件的引用与创建必须对得上」
- 第 16 组「main() 的接线必须完整」

两组都是为具体事故加的，也**都做过反证**（删掉对应代码，测试当场报错）。

第 15 组中间还报过一次假警（文档字符串里的示例被当成真代码），用 `tokenize`
剥掉注释和字符串才修对 —— 扫描器先剥注释是基本功。

另外把自检里的固定端口改成让系统分配：原来那个端口撞上 Windows 为 Hyper-V
预留的排除区间，直接 `WinError 10013`，而报出来是「自检失败」，让人以为是
代码坏了 —— **测试基础设施的问题伪装成了产品问题。**
---

## 1.7.2

**1.7.1 的图形界面打不开，请直接用这一版。** 下面说明是怎么回事。

### 1.7.1 坏在哪

往 `main()` 里插「首次运行自动生成配置」那段代码时，我用了一次赋值去取目标行
的文本 —— 那次赋值真的写进了数组，于是

```python
        app = App(root)          # 保持引用：mainloop 阻塞期间它一直存活
```

被一行注释顶掉了。**`App` 从此没有被构造过。**

它的表现极具迷惑性：

- 进程活着，窗口也在，**不报错、不弹窗、日志干净**
- 只是窗口停在 Tk 的原始根窗口上 —— 标题还是 `tk`，尺寸 216×239，一片空白

也就是说，一个只看"有没有崩溃、日志有没有报错"的检查**完全发现不了它**。
这次是靠截图看窗口标题才挖出来的。

### 自检加了第 16 组

「`main()` 的接线必须完整」：查它有没有构造 `App`、有没有建 `mainloop`、
**以及顺序对不对**（先补配置，再建界面）。

第 15 组查的是"控件有没有建"，这一组查的是"界面有没有被构造" —— 两层不同，
上次的 bug 恰好从第 15 组底下漏过去。

**这组测试自己也验过**：把 `app = App(root)` 换成 `app = None`，它当场报 2 项失败。

### 顺带：自检不再写死端口

假 NapCat 用的固定端口撞上了 Windows 为 Hyper-V 预留的排除区间，
直接 `WinError 10013`。而报出来是「自检失败」，让人以为是代码坏了 ——
**测试基础设施的问题伪装成产品问题**。改成让系统分配空闲端口。

### 网盘分享版

新增 `dafeiyu-netdisk.zip`：内置 NapCat，解开双击 `首次使用-点我.bat` 就能用，
不用自己去下 NapCat。

顺带修了 NapCat 的入口脚本 `loadNapCat.js` —— 它里面烤着打包者的**绝对路径**
（含用户名），既泄露又导致别人解压后跑不起来。改成相对 `__dirname` 解析。

---

## 1.7.1

### 三个 bug

**弹窗刷屏。** 上一版删立绘时，我把三个头部标签的**创建**整段删掉了
（它们当时正好夹在立绘代码和主题按钮之间），但引用还在。

后果不是崩溃 —— 界面照常启动，只是每个引用它们的回调都撞 `AttributeError`：
`_paint_header` / `_poll_trigger` / `_poll_health` / `done` 全部中招，
用户看到一连串错误弹窗。**比崩溃难查得多**，因为一切"看起来正常"。

**日志时间戳差一秒。** `log()` 里 `datetime.now()` 被调了两次（一次给控制台、
一次给文件）。两次调用正好跨过秒边界时，文件里的外层时间就比消息自带的内层
时间晚一秒：

```
[2026-09-19 13:09:35] [13:09:34] [INFO] 界面已启动
```

概率约百万分之六 —— 罕见、偶发、看起来像日志错乱。实测旧写法
**70 万次里错位 4 次**。改成取一次、用两次，结构上不可能不一致。

**配置提示答非所问。** 从源码目录直接运行时报「请把 config.json 放在程序
同目录」，可用户多半只是想打开程序、却点到了仓库里的 `gui.py`。现在会分辨
两种情况，直接告诉他该双击 exe。

### 二次元化

配色换成天依蓝。**这不是挑的，是算的**：

```
#66CCFF 压白字  →  1.80:1   ❌ 浅色主题当按钮底色根本看不清
#66CCFF 在深底  →  9.14:1   ✅ 深色主题当强调色非常漂亮
```

所以两套主题用不同的蓝：浅色 `#2B8FC7`（3.59:1），深色 `#66CCFF`。

**边缘二次元，内容克制** —— 配色、渐变、头图只在头部和强调色上做文章，
正文的字号、对比度、留白一律不动。装饰铺进正文是二次元 UI 唯一的砸法。

### 整条头部可以换成一张图

程序目录放 `header-light.png` / `header-dark.png` 就生效，不用重新打包。

**为什么非得两张**：文字颜色在两套主题里是相反的（浅色深字、深色白字），
需要的是**两个方向的遮罩** —— 一张图靠调透明度盖不住两个方向。

遮罩的形状也是算过的：标题占 x `3%~38%`，角色的脸在 `49%`。线性淡出到
38% 处还有约 35% 不透明，字会发糊、还会洗到她脸上。所以改成**先实后淡**。

### 日志区

原来 `expand=True` 把中间全吃掉 —— 两行内容占一屏。改成自适应：
内容少就矮（下限 5 行），窗口矮就缩，上限 16 行。

滚动条也换了。clam 默认是「上箭头 + 轨道 + 横纹握把 + 下箭头」四段拼的，
九十年代的样子 —— **光设颜色治不了，那些是布局里的独立元素**，
得 `style.layout()` 整个换掉。

改完发现扁平到几乎看不见，才想明白真正的毛病不在长相：**装得下就不该挂着
滚动条**。现在按需出现。

### 字体

系统里没有中文圆体（Windows 的常态）。把五个候选**用真实界面文字渲染出来
比对**，`OPPOSans R` 最接近「可爱但不花哨」。做成三轮降级链。

须说明：**它不是 Windows 自带的**，别的机器上会退到 `Noto Sans SC` ——
不会变方块，但看起来会和你不一样。

### 自检：14 组 → 15 组

新增「控件的引用与创建必须对得上」：静态扫源码 + 动态建界面逐项 `hasattr`。

**这组测试自己也验过** —— 把某个控件的创建改个名，它当场红。

中间它报过一个假警（`self.lbl_xxx` 出自文档字符串），用 `tokenize` 剥掉
注释和字符串才修对。扫描器先剥注释，是基本功，我漏了。

---

## 1.7.0

按任务书做的五项 P0。**先说一句前提**：任务书参照的是一份较旧的仓库快照——
它说「缺少统一测试体系」，而实际上已经有 100+ 项断言的自检；它只提
`live_notify.py` 过大，而那 1782 行比 `gui.py` 的 3325 行还小一半。
所以下面几项是按**实际代码**重新核过之后才动手的。

### P0-4 模板占位符校验

**这是唯一会「对外出丑」的失败。**

模板里拼错一个字母（`{titel}`），`format()` 抛 KeyError，被兜底逻辑吞掉，
最后把 `{titel}` **原样发进 QQ 群**——日志里只有一行 WARN，群友已经看见了。

两道防线：加载时校验（`check` 里列出来）+ 渲染时清理（认识的留着，其余删掉）。

写断言时又抓出两个缺口：**落单的花括号**（`{ 畸形`）能活着发出去；修的时候
一刀砍过头，把 `{title}` 自己的括号也削了。两条都是断言先发现、代码后修的。

### P0-1 单实例锁

同时跑两个 watch，两边都在监听、都在发送——群里收到双份，用户不知道为什么。

用 `msvcrt.locking` 而不是「写个 pid 文件」：pid 文件在崩溃或被任务管理器强杀
之后会留下来，下次启动就被永久锁死，只能让用户手动删。**msvcrt 的锁由内核
持有，进程一死立刻释放。** 自检里专门有一条把子进程强杀，验的就是这个。

过程中撞了两个 Windows 的坑，都靠对照实验才看清：**`_locking` 锁的是整个
文件，不是那一个字节区间**——所以「锁加在偏移 4096、信息写在偏移 0」从根上
就不成立。两者只好分成两个文件。

### P0-3 控制端口认证

原来只认 URL 参数、用 `==` 比较，而且启动时**把完整 token 直接打进日志**。

现在优先认 `Authorization: Bearer` 头，一律 `hmac.compare_digest`，URL 参数
只作兼容模式（用一次提示一次），启动日志不再输出 token。

顺带修了一个从没被发现的 bug：**控制页的按钮是 `fetch('/trigger')`，不带
token，所以只要设了 token，那个按钮一直是坏的**（403）。现在页面上有输入框，
token 存 sessionStorage，走 Authorization 头。

### P0-2 触发成功与送达成功分离

问题在 `fire()` 那十几行：冷却门闩先标记、直播状态先认定、`send_to_groups`
的**返回值直接丢掉**，然后一律 `return True`。**于是「一条都没发出去」和
「全部发成功」在调用方看来长得一模一样。**

现在有了 `SendResult`：分清 `delivered` / `partial` / `lost`，带上失败群、
重试轮次、原因。全丢时打一条显眼的 ERROR，并且明说「不会自动重发」。

注意：**开播状态照记不误**。事件确实发生了，不能因为没发出去就说没开播——
要分开的是「事件」和「送达」，不是把所有东西一起否掉。

### P0-5 配置安全检查

`check` 多了第 6 节，专门抓「配置看着没问题、实际会出事」的值：

- `message.link` 还是示例里的 `YOUR_ROOM_ID` —— **会被原样发进每一个群**
- 控制端口开着却没设 token
- `dry_run` 开着（以为在发，其实一条都没发）
- 勾了「直播间轮询」却没填房间号

输出里**绝不打印任何密钥的值**。

### 自检：10 组 → 14 组，114 项断言

新增模板校验、单实例锁、控制端口认证（真的起端口打 HTTP 请求）、配置安全检查。

其中修了一个会**炸掉整个测试进程**的问题：Tk 根窗口 `destroy()` 之后对象还挂着，
等到后面多线程小节才被 GC 回收，撞上
`Tcl_AsyncDelete: async handler deleted by the wrong thread`。必须显式
`del` + `gc.collect()`，而且要在创建它的线程里做。

### 没做的部分

任务书里的 P1 我建议缓办，理由写在动手前的评审里：

- **P1-3 抽象外部服务接口 —— 建议不做。** 现在的自检是拿真 HTTP 打本地假
  NapCat，这比 mock 掉客户端**测得更真**（HTTP 编解码、重试、错误分支全真跑）。
  换成接口抽象会变成 mock 测 mock，覆盖面反而下降。
- **P1-1 拆分 `live_notify.py`** —— 任务书提议 11 个模块 + `triggers/` 包，
  而那个文件只有 1782 行，平均每模块 150 行，是碎片化不是解耦。真要拆，沿
  真实接缝拆 3~4 个就够。
- **P1-2 统一状态机** —— 它动的是程序的心脏。先补状态转移的断言把现有行为
  钉死，**再**谈重构。
- **P1-4 dataclass 配置** —— 任务书要求「内部结构化、外部兼容字典」，那是
  同时维护两套表示，复杂度增加而非减少。

---

## 1.6.6

### 深色模式下标签栏和滚动区发白（修）

**根因是 Python 的默认参数求值时机。**

    def __init__(self, parent, background=BG, ...):

`BG` 在**模块导入那一刻**就被求值烤死了，之后 `apply_theme()` 再改全局量也
追不回来 —— 这两个控件永远停在初始主题的颜色上。`ScrollFrame` 和 `TabStrip`
各有一处，深色模式下就是「标签栏发白、滚动区底下一整片白」。

改成 `background=None`、构造时再取。同一文件里的 `ToggleSwitch` 和
`ThemeToggle` 本来就是这么写的，所以它们从头到尾没出过问题。

实测亮像素占比：**38.5% → 8.8%**（剩下的 8.8% 全在 Windows 自绘的标题栏上，
不归程序管）。

### 加了第 10 组自检

静态扫描 `gui.py` 的所有函数签名，抓 `=BG` 这类写法。它不针对这一个 bug，
而是针对**这一类** —— 以后谁再这么写，自检当场红。

配 8 项动态断言：深、浅两套主题各建一次 `ScrollFrame` 和 `TabStrip`，
验证底色确实跟随主题。

**这组检查自己也验过**：拿 3 个该抓的样例和 4 个不该误伤的样例试了一遍，
全中，没有漏也没有误伤。

---

## 1.6.5

### 顶栏显示版本号

以前要鼠标悬停在 exe 上才看得到，现在标题右侧直接显示。

### 源码顶部加了一段说明

写给以后翻到这个文件的人 —— 解释这个程序为什么一个第三方库都没用。

---

## 1.6.4

### 启动豁免期：拦住鼠标误触

实测踩到过两次（主题开关、标签栏）：**程序启动时如果鼠标恰好停在某个控件上，
Windows 会把光标位置上那一次点击投递进来**，那个控件就被"点"了一下。

表现是「程序自己动了一下」——主题莫名其妙切了、页面莫名其妙跳了。极难自行
诊断，因为用户根本没点。

现在启动后 **0.8 秒内的点击一律不认**。

**装在四处点击入口上，不装在 `App` 的方法上。** 这一点很关键：`toggle_main`、
`select_tab` 这些方法在构建界面时也会被程序自己调用，装在方法上会把初始化
一起拦掉——第一版就是这么写的，测试当场红了。

### 顺带修掉一个更隐蔽的错

第一版把计时起点放在了 `App.__init__` **开头**。而构建界面本身要花几百毫秒，
等窗口真正出现时豁免期早就过去了，**等于没装**。

误触是「窗口出现的那一刻」发生的，计时就得从那一刻算——现在挪到 `__init__`
末尾、`mainloop` 之前。

这条是测试逼出来的：如果只靠肉眼验收，它会一直"看起来生效"，实际上毫无作用。

---

## 1.6.3

### 「未开启」挪到主按钮正下方

它原本挂在顶栏右上角——离那个按钮隔着整个窗口。可它描述的恰恰就是那个按钮
控制的东西，读起来是两回事。

现在它是按钮的下一行，紧贴着：

```
[ 开始直播通知 ]
     未开启
```

顶栏因此空出一行，只剩标题、QQ 状态和触发源，反而清爽了些。

---

## 1.6.2

### 默认窗口尺寸改成 780 × 915

比原来的 860 × 720 **更窄更高**：

- **窄** —— 窗口铺太开时每行字都拉得很长，反而不好读
- **高** —— 下面那张日志面板需要纵向空间

仍然跟屏幕尺寸取 `min`，小屏笔记本上缩得下；最小尺寸放宽到 740 × 560。

---

## 1.6.1

### 主题开关改成圆形图标按钮

原来是一枚写着「深色模式」的胶囊按钮 —— 占地方，而那四个字本身没什么信息量。

现在是 30px 的圆形按钮，里面一枚字形图标：

- 浅色时显示**月亮**（点了变深色）
- 深色时显示**太阳**（点了变浅色）

图标表示**点下去会变成什么**，这是开关类控件的通行做法。

### 字形是实测挑出来的，不是拍脑袋

Tk 8.6 对 emoji 的支持一直很差，所以把候选字形 × 候选字体全渲染出来比对了一遍：

| 字体 | ☀ U+2600 | 🌙 U+1F319 |
|---|---|---|
| **Segoe UI Symbol** | 实心太阳 ✅ | 月牙 ✅ |
| Microsoft YaHei UI | 实心太阳 ✅ | 月牙 |
| **Segoe UI Emoji** | **空心圆圈 ❌** | 月牙 |

**字体必须指定 `Segoe UI Symbol`** —— 用 `Segoe UI Emoji` 的话 ☀ 会变成一个
空心圆圈，🌞 更会变成一个笑脸。

之前 `☾` 显示成怪模怪样的 C，不是字形的问题，是 **9pt 太小**、月牙糊成一团。

---

## 1.6.0

### 内置文案池，自动轮换

原来只有一套文案，翻来覆去就那一句 —— 群里刷到第三遍就自动忽略了。

我先做了一版「文案库 + 让用户自己挑」的界面，但那是**把活儿推给用户**：
谁有闲心一套一套憋十句出来。改成**程序自带一池、自己轮换**，用户什么都不
用做：

| | 套数 |
|---|---|
| 开播 | **12 套**（直白 / 卖萌 / 中二 / 自嘲 / 简洁 / 深夜 / 周末 …） |
| 下播 | 6 套 |
| 二次提醒 | 6 套 |
| 换游戏 | 5 套 |

每次发送**重新挑一条**，所以同一场直播里的开播、二次提醒、下播会各挑各的，
不会整场都用同一句。

**用户自己写的那句排在最前面**，不会被内置池子盖掉 —— 那是他一个字一个字
敲的，是这套文案里最有个性的一条。想加自己的就往文本框里添，用单独一行
`---` 隔开。

界面上那个「挑选 + 加进去」的下拉去掉了，换成一行提示告诉你现在有几套。

### 界面文字精简

之前每张卡片下面都挂两三行说明，一页看下来全是字。原则改成：**界面上
一行说完，说不完的进文档**。举几个例子——

| 原来 | 现在 |
|---|---|
| 不管用 OBS、直播姬、直播伴侣还是手机开播，只要房间真的开了就能检测到。 | 用什么软件播都认，手机播也认。 |
| 打开软件 ≠ 开播。你开软件后还要调设备、试麦，这段时间会白提醒群友一次，所以默认关闭。 | 打开软件 ≠ 开播，会白提醒一次。默认关。 |
| 点一下就开始，之后一直挂着就行。程序跑在独立的 QQ 副本上，你自己聊天的 QQ 不受影响。 | 点一下就开始，之后一直挂着。不影响你自己聊天的 QQ。 |

占位符说明原本出现两次，合并成一行。

### 窗口收窄

980 → **860**（最小 780）。原来横向铺得太开，每行字都拉得很长，反而不好读。

---

## 1.5.2

### 「未开启」不该跟主题按钮排一起（修）

上一版为了修对齐，把监控状态和主题按钮塞进了同一行 —— 那是**拿视觉便利
换语义**：

- 「未开启」说的是**监控开没开**（大按钮控制的那个）
- 「深色模式」是**界面偏好**

两者毫无关系。并排放会让人以为「未开启」修饰的是旁边那个按钮，或者以为
它们属于同一组设置。

现在分组按语义来：

```
大肥鱼直播姬                                  深色模式     ← 偏好，独立一行
● QQ 未就绪（点下面的按钮会自动启动）          未开启       ← 状态归状态
触发源　监控未开启
```

**对齐是为了让相关的东西看起来相关**，不是让所有东西都排成一条线。

---

## 1.5.1

### 胶囊按钮上有条缝（修）

按钮右侧有一条贯穿整个高度的白缝，左右两个圆头像是**拼**上去的。

根因：那个按钮是「四个角贴抗锯齿小图 + 中间用矩形填」拼出来的。
**Tk 的 `create_rectangle` 是硬边、按整数像素填充的**，而角落小图是按覆盖率
跟背景混过色的 —— 两种边缘模型根本凑不到一起，拼得再准也会在接缝处露馅。
上一版只把缝从"锯齿"改成了"细线"，没解决。

现在**整块按钮画成一张抗锯齿图片**，边缘只有一处过渡，没有接缝可言。
覆盖率掩码按尺寸缓存，颜色变化时只做混合：实测单帧 **0.63ms**（预算 16ms），
首次生成一张新图 17.6ms、命中缓存 0ms。

### 主题按钮跟标题没对齐（修）

它垂直中心在 y=30、标题在 y=34；而且状态文字在第二行，两个都右对齐却各飘
各的。现在三个元素在**同一条基线**上：

```
大肥鱼直播姬                          未开启  [深色模式]
```

### 顺带

- 补了一条测试断言「圆角边缘存在半覆盖像素」—— 走回"矩形 + 椭圆"的画法
  覆盖率只会有 0 和 1，立刻红，防止以后有人「简化」回去

---

## 1.5.0

### 我先去看了真实应用长什么样

之前几版我都是闭门造车，这次把真实应用的截图**下载下来逐张看**（这一步早就该做）：

- **DevToys**（上架的 WinUI 3 应用）
- **FluentTerminal** 的设置页
- **Notepads**
- **Google 的 Now in Android**（官方 M3 参考应用）
- 微软官方的 Fluent 材质原型图

看完立刻发现三个根本性错误。

### 一、根本不该有卡片边框

FluentTerminal 的设置页、Windows 11 自己的设置，**分组全靠留白 + 一个标题**，
根本不用框。DevToys 只在「一格格工具入口」那种地方用卡片，而且卡片是
**圆角 8px、比背景略亮、没有描边**。

而我给每个分组套了一圈 1px 描边，一页叠七八个框 —— 这是整个界面最土的地方。
现在全去掉了。

### 二、开关应该是胶囊滑块

Windows 11 的开关是**胶囊滑块**，不是方框打勾。这是最一眼可辨的 Windows
元素，用 `Checkbutton` 会立刻显得像十年前的软件。自己画了一个，8 处全换掉。

### 三、动效应该用弹簧，不是缓动曲线

Material 3 Expressive 最核心的东西不是颜色，是**物理弹簧**。参数直接抄官方
token（`pub.dev` 上的 `M3ESpring`）：

| Token | damping / stiffness | 过冲 | 落位 |
|---|---|---|---|
| `expressiveSpatialFast` | 0.6 / 800 | **+8.9%** | 384ms |
| `expressiveSpatialDefault` | 0.8 / 380 | +1.3% | 448ms |
| `expressiveEffectsFast` | 1.0 / 3800 | 0% | 176ms |

官方还有一句关键的话：**效果类动画两套方案的参数完全一样** ——
「表现力属于移动，不属于颜色」。所以位移用 spatial（会弹），颜色用 effects
（不弹，弹了反而眼花）。

圆角也按官方的 shape scale 对齐：`4 / 8 / 12 / 16 / 28 / full`，
主按钮改成胶囊（M3 的按钮就是 full radius）。

### 顺带修掉的

- **弹簧积分在高刚度下数值爆炸**：stiffness 3800 时阻尼系数 c≈123，
  按 1/60 秒积分 `c·dt≈2.06 > 2`，一步就翻号，实测峰值冲到 3×10²⁰。
  改成 1/600 秒的子步积分。
- **主题开关的圆角看不见**：抗锯齿圆角是拿「背景色」去混的，而它坐的头部是
  浅灰、传进去的背景却是白 —— 白混白还是白，看着就是个直角方块。
- 主题开关里的 `☾` 在微软雅黑下渲染成一个怪模怪样的 C，改用纯文字。
- `select_tab` 读 `pady` 时 `cget` 会返回 `Tcl_Obj`，直接 `int()` 会炸。

---

## 1.4.2

### 按钮的圆角是锯齿的（修）

**tkinter 的 Canvas 不做抗锯齿。** 之前用「两个矩形 + 四个椭圆」拼圆角 ——
那是 Tk 界的经典做法，但它画出来是**硬边**的：半径一大，四个角就是肉眼
可见的台阶，看着像马赛克。半径 12 已经很明显了。

改成逐像素算覆盖率（4×4 超采样），再把边缘像素跟背景色按比例混一下，等于把
抗锯齿**烘焙进图片**。只画四个 r×r 的小角，中间照旧用矩形填。

性能不用担心：实测单帧重绘 **0.58ms**，而一帧的预算是 16ms。覆盖率矩阵跟颜色
无关，缓存一次反复用；角落图片按 (半径, 角, 填充色, 背景色) 缓存，动画每帧换
一个颜色也扛得住。

### 测试

`_selftest.py` 加了 11 项圆角测试，断言的是「边缘存在**半覆盖**的像素」——
走回椭圆画法的话覆盖率只会有 0 和 1，立刻红。这是防止以后有人「简化」回去。

---

## 1.4.1

修三处「看着别扭」的地方。

### 标签栏换成自绘的

原来是 ttk.Notebook 自带的标签页，而 clam 主题的 tab **自带竖向分隔线和一圈
虚线焦点框** —— 那是画在 tab 元素本身上的，把 bordercolor / lightcolor /
darkcolor 全设成背景色也去不掉，只改颜色没用。

现在自己画：一行 Label + 一条会**滑过去**的指示条。顺手多了一个动效 ——
选中态滑过去是 Fluent 里很典型的一笔。

### 主题开关做成按钮，顺带修了对齐

之前它是**没有边框的一行字**，而 Label 的 `padx` 会把文字往里推，于是「浅色」
比下一行的「未开启」缩进了 8px —— 就是「文字没对齐」。

加了边框之后，按钮的**右边缘**跟「未开启」的右边缘对齐；同时也一眼看得出
这是个能点的按钮，而不是一行说明文字。

### 表单标签没垂直居中

`row_label` 用了 `sticky="nw"`（顶对齐），「标题」两个字贴着输入框上沿，
而输入框比它高 —— 看着就是没对齐。改成 `"w"`（垂直居中）。
多行输入框（开播文案）例外，那种标签按惯例应该顶对齐，留了开关。

顺带把间距统一到 12px（Fluent 的间距档位是 4/8/12/16/20/24），
卡片标题从主色改成正文色 —— Windows 11 设置里的分组标题就是加粗深色，
彩色标题会让每个卡片都在喊「看我」。

### 测试

补了 12 项**动效行为**测试。截图只能证明"没报错"，证明不了"真的在动"，
所以这些断言的是颜色停在中间值、状态点采到十几种不同颜色、指示条 x 坐标
真的变了。

---

## 1.4.0

配色换成 Fluent 骨架 + 降对比，并给界面加上动效。

### 之前错在哪

上一版结构是对的（大标题、常驻底栏、系统语义色），但**颜色照搬了 Apple**：
Apple 深色模式的底是纯黑 `#000000`、浅色卡片是纯白 `#FFFFFF`。纯黑配纯白
对比过强，那正是「刺眼」的来源。而 Windows 走的是相反的路子。

### 现在照 Fluent 来

参考微软官方的 [Fluent 材质](https://learn.microsoft.com/windows/apps/design/signature-experiences/materials)
规范，底色用 `#1F1F1F`（官方是 `#202020`）而不是纯黑，卡片 `#2A2A2A` 亮一档，
靠**亮度差**分层。在此之上再降一档对比：正文不用纯黑纯白，次要文字压到中灰，
分隔线淡到几乎看不见。

| | 之前（Apple） | 现在（Fluent 降对比） |
|---|---|---|
| 浅色底 | `#F2F2F7` | `#F7F7F7` |
| 深色底 | `#000000` 纯黑 | `#1F1F1F` |
| 深色卡片 | `#1C1C1E` | `#2A2A2A` |
| 主色 | `#007AFF` 系统蓝 | `#2B7FD4` 降饱和一档 |

### 动效

时长和缓动抄自微软官方
[Timing and easing](https://learn.microsoft.com/windows/apps/design/motion/timing-and-easing)，
**不是自己拍脑袋定的**：

| 场景 | 时长 |
|---|---|
| 状态切换（开始 ↔ 停止） | 250ms |
| 切换标签页 | 167ms |
| 悬停 / 按下 | 83ms |

缓动两条，方向不能反：进入用 `cubic-bezier(0,0,0,1)`（快进慢停），
退出用 `cubic-bezier(1,0,1,1)`（慢起快走）。tkinter 没有 CSS transition，
只能用幂函数逼近这两条曲线，再用 `after()` 逐帧驱动。

具体做了四件：

1. **按钮颜色过渡** —— 悬停 83ms、状态切换 250ms，不再是硬切
2. **底栏状态点呼吸** —— 监控中用连续正弦而不是"亮-暗"两帧硬切；硬切看着像
   在闪，像报警，呼吸才像"我在正常工作"
3. **切换标签页内容落位** —— tkinter 做不了透明度淡入，改用位移
4. **「上次通知」结果变化时描边闪一圈高亮** —— 只动 outer 一个控件的底色
   （它就是那圈 1px 描边），代价极低

所有动画共用一个 `Animator`，好处是换主题重建界面时能一把取消干净 ——
否则旧控件的回调会打到已销毁的 widget 上。

**做不到的说清楚**：真正的透明度淡出、毛玻璃 Mica/Acrylic，tkinter 访问不到
窗口合成器，做不了。没有假装能做。

### 顺带

- 抓到一个潜伏的 `NameError`：`import math` 那次编辑失败了没补上，而
  `_pulse_dot` 只在进入监控状态时才跑 —— 截图测试永远碰不到，一动
  「开始直播通知」就炸。补了动画行为测试才暴露出来

---

## 1.3.1

### 日志文件里的密钥打码

日志文件里一直躺着两个明文密钥：NapCat 的 WebUi token，和本项目控制端口的
触发 token。它们等价于「谁拿到谁就能操作」，而日志恰恰是**拿来外传的**
（贴给群友、发给别人看）—— 之前两次排查问题，日志里都带着它们。

现在**落盘的那份**会把密钥打成 `d820af…（已打码）`，只留前 6 位够核对是哪一份。
控制台和界面日志面板保持完整，因为用户要在那儿复制触发地址。

打码规则特意做了误伤检查：`日志已保存到 C:\token\abc.txt` 这种带 token
字样但不是密钥的行，原样保留。

---

## 1.3.0

两件事：**通知发不出去会自动重试**，以及界面按 Apple Music 的思路重排。

### 发送失败会重试了（重要）

1.1 的日志里抓到过一次真实事故：

```
22:32:40  NapCat 已连接
22:32:55  触发原因：检测到直播软件启动：livehime.exe
22:32:57  [失败] 111111111 -> 连不上 NapCat
22:33:02  [失败] 222222222 -> 连不上 NapCat
22:33:07  [失败] 333333333 -> 连不上 NapCat
22:33:07  完成：成功 0/3
```

**三条全丢，没有任何补发**，界面也不吭声。而 15 秒前它才说过「NapCat 已连接」。
那正好是这个工具最不能出错的一刻 —— 开播了，群里一个人都没收到。

现在：发失败的群会按 **5 / 15 / 45 秒**退避重试三轮，**发成功的不重发**
（否则群友会连着收到好几条一样的）。三轮都失败才认输，这时：
- 日志里记 ERROR
- 顶栏亮出告警
- 弹一次提示，告诉你按快捷键可以手动补发

界面「运行日志」页底部新增**「上次通知」**卡片，一眼就能看到上一批发出去没有。

### 界面重排（参考 Apple Music）

之前那个毛病一直没根治：**主操作是一个横跨整屏的大色块**，从 92px 收到 58px
也只是没那么吵而已。这版按 Apple Music 的结构来：

- **主操作搬进常驻底栏** —— 左侧是当前状态，右侧是唯一的主按钮。Apple Music
  从不用大按钮；而且这样切到任何标签页按钮都还在，不用滚回去找。
  （按钮放底栏右侧，也正好是 Windows 对话框的习惯。）
- **标题放大**到 23pt，像 Apple 的 Large Title
- **配色换成 Apple 的系统语义色**：systemBlue / Green / Orange / Red，
  底色是 systemGroupedBackground 的中性灰。深色模式照 Apple 的规矩来：
  底是纯黑，卡片 #1C1C1E，靠**亮度差**表达层次
- 标签栏在深色下会露出一圈浅色描边（clam 主题画在 tab 身上，只设
  TNotebook 不够），已去掉

### 照顾 Windows 的习惯

- **窗口大小和位置记住了** —— 下次打开还在原来的地方。最大化时不存尺寸，
  否则下次会以最大化尺寸开出来还缩不回去
- 屏幕小的时候按屏幕实际尺寸挑初值，不再硬编码 980x760（在 1366x768 上会顶到任务栏）
- 间距统一到 **8px 栅格**（16 / 24 / 48），不再是一堆拍脑袋的 13、15、22

### 顺带

- 「运行日志」页的日志框原来独占整页，内容才两行却撑满一屏，现在收进
  合理尺寸，空出来的地方放了「上次通知」卡片
- 主题开关在**鼠标正好停在它上面时启动**偶尔会被触发一次 —— 已确认是
  光标位置导致，不是循环

---

## 1.2.0

界面重做，外加一个深色模式。

### 深色模式（新增）

右上角一个开关，点一下就在浅色和深色之间切，选择会记住。

参考的是 Flyme 的「夜间模式」：**深邃但不纯黑**，而且越靠上的层越亮
（窗口底最暗 → 卡片略亮）。纯黑配纯白对比过强，晚上看久了眼睛累。

### 界面重做

配色推倒重来。前两版的毛病是一样的 —— **拿背景和按钮抢戏**：第一版是
冷蓝灰的工程软件味，第二版改成一整块高饱和紫渐变加一个 92px 的巨大按钮，
更吵。

这版按 Flyme 设计语言里那两条来：

> 突显内容 —— 精妙的设计不应喧宾夺主
> 恰到好处的圆角 —— 少了些许锋锐，多了一丝温和

具体是：

- 底色改成**中性**灰白，不再带明显色相
- 主色只出现在真正要引导视线的地方（动作按钮、选中态），不铺满界面
- 主按钮从 92px 高收到 58px，字号从 19 降到 15 —— 尺寸该由内容决定，
  不是由「要显眼」决定
- 分层改用发丝级描边和留白，去掉重边框
- 顶部那条高饱和渐变横幅换成几乎看不出的明暗过渡 + 一条分隔线

### 顺带修掉的

- **打包后的日志其实一直没写下来**。`live_notify.py` 用 `__file__` 算数据目录，
  而打包后那是 PyInstaller 的**临时解包目录**，程序一退出就删 —— 也就是说
  从 exe 运行时，日志全进了临时文件夹。`app\logs\` 里只有源码运行时的记录，
  点「打开日志文件夹」看到的是一片旧数据。改成跟 `gui.py` 一样相对 exe 解析。
- **技术文档「已知限制」有三条在说谎**：「直播姬无法自动检测」其实能了
  （直播间轮询不看软件）、「QQ 副本 1.1 GB」其实是 7 MB、`games.py`
  号称的第 5 层识别根本没实现
- **`config.example.json` 缺了 `game` / `reminder` 两整段**，照着它抄会缺功能
- 开关和输入框的颜色是写死的白色，深色主题下会变成刺眼的白块
- `games.py` 的手工映射表缓存**按时间**判定，五分钟内改了配置不生效，
  改成按内容判定
- 主题开关原本画在 Canvas 上当文字项用 `tag_bind` 绑点击，实测出现过
  **主题自我横跳**（一秒切一次）。换成真正的控件，并加了防重入

### 新增测试

`_selftest.py` 补了 25 项针对 `games.py` 的纯逻辑测试（标题清洗、通用源名
判断、窗口串解析、认不出来时不能抛异常）。这个模块 617 行、是 1.1 里最大
的新增部分，之前**零覆盖**。

---

## 1.1.0

开播通知从「我开播了」变成「我开播了，正在玩《三角洲》」。

### 游戏名识别（新增）

通知里自动写上当前在玩什么。五层依次尝试，任意一层命中就停：

```
手工映射表 → 场景字典 → 平台库 → 窗口标题 → exe 版本信息
```

- **不用 OCR。** 实测窗口标题就够了：三角洲这类反作弊游戏读不到 exe 路径、
  版本信息也是空的，但窗口标题给的正是「三角洲行动」，一层命中
- **场景字典是最划算的一层**：直播姬和 OBS 的场景文件里，每个游戏捕获源
  都记着 `<窗口标题>:<窗口类>:<exe名>`，等于一份你自己维护出来的游戏清单
- **不是「哪个游戏在跑」，是「哪个窗口像在玩的游戏」**：遍历全部可见窗口
  按窗口类 / 全屏 / 内存打分。只看前台窗口不行——你切出去聊天时前台就变成
  浏览器了
- 标题清洗：某些启动器会往标题里插零宽字符（`C​a​l​l​ ​o​f​ ​D​u​t​y`
  每个字母之间一个 U+200B），不清掉的话群里会收到一串看不见的乱码
- 界面里有「测试一下现在认成什么」按钮；认错了可以填进「不算游戏的」

### 封面小图（新增）

开播通知里附一张直播间封面。

**不下载、不缩放、不依赖 Pillow**：B站图床支持在 URL 后面缀缩放参数，
由 CDN 直接切好返回。实测原图 155 KB，`@200w_112h_1c.webp` 只有 6.4 KB。

### 二次提醒（新增）

开播 30 分钟、1 小时各补一条，给第一波没看到的人第二次机会。

- 本场硬性上限（默认 3 条，含开播那条），断流重连刷不爆
- 只有直播间确实还开着的时候才发
- 默认不 @ 任何人

### 换游戏播报（新增）

中途换游戏自动补一条「换游戏了，现在打《XXX》」，带 5 分钟冷却，
同样不 @ 任何人。

### 文案多套轮换（新增）

开播文案可以写好几套，用单独一行 `---` 隔开，每次随机挑一套。
每次都发一模一样的话，群里刷到第三遍就自动忽略了。

### 下播总结带人气峰值（新增）

下播消息里可以写 `{peak}`，自动填本场人气最高值。

### 占位符「整行消失」（新增）

`{game}` / `{peak}` 没值时，把它所在的**整行**删掉，而不是渲染成
「正在玩《》」。整条消息被删空时会退回未删行的版本，绝不发空消息。

### 界面

「消息与设置」页新增游戏识别、二次提醒两组设置，文案框支持多套。

---

## 1.0.0

首个版本。

开播时自动往 QQ 群发 `@全体成员`，下播也发一条。

### 触发

- **直播间状态轮询**（推荐）—— 与开播软件无关，手机开播也认
- **OBS 推流事件** —— 精确到按下「开始推流」那一刻
- **全局快捷键** —— 手动兜底
- **进程检测** —— 默认关闭（打开软件 ≠ 开播）

### 发送

OneBot v11 HTTP → NapCat → QQ 群。多群逐个发、带间隔、有冷却闸门，
同一次开播不会被多个触发源发两遍。

### 不占用你自己的 QQ

给 NapCat 一份路径不同的 QQ 副本，Windows 把它当成另一个应用，于是成为
第二个互不干扰的实例。停止时只按路径过滤，**绝不碰你自己的 QQ 进程**。

### 零依赖 · 绿色免安装

只用 Python 标准库 + tkinter。连 WebSocket 客户端和 PNG 编码器都是手写的。
exe 里打包了 Python 运行时，目标机器什么都不用装。

### 隐私

除查询你自己的开播状态外全部本地通信。聊天内容不落盘。
1.1.0 的游戏识别同样是本机读窗口和场景文件，不截图、不上传画面。

### 开发过程中顺带解决的

- **部署体积 1204 MB → 41 MB**：私有 QQ 副本改用目录联接，只保留约 8 MB
  顶层文件；另外删掉 NapCat 里 Linux/macOS 用不到的原生模块
- 界面整体重做（深蓝横幅 + 卡片布局 + 可滚动设置页）
- 三份文档拆分为使用 / 技术 / 开发
